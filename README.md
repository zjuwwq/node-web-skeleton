node-web-skeleton
=================

a node web skeleton which share code between server and client

# Requirements
- nodejs

# Preparation
- run `npm install`

# Testing
- run `gulp`
- run `npm start`
